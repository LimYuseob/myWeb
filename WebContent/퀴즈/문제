파일명은 findLetter.html

랜덤하게 1 ~ 127 까지 정수를 생성해서, 해당 정수에 대응하는 아스키 문자로 변환하여 변수에 담으세요
즉 위의 경우가 모두 정상적 이라면 변수가 2개 여야 합니다.

구글링 해서 여러분이 찾아야 할것은, 아래와 같습니다.

1. 정수에 해당하는 Ascii 문자로 변환할 수 있는 API(String 객체에 메서드) 를 찾아서
발생된 수의 값을 문자로 변환하세요.

위에서 발생된 랜덤한 문자는 조건을 통해서 아래와 같이 출력 되어야 합니다.

case.1 아스키 코드에 보면 0 ~ 9 까지의 숫자를 나타내는 코드가 있습니다.
이보다 적은 값인 경우엔 발생되었을 경우엔 숫자보다 작은 특문[문자(ascii 코드값] 입니다.
라고 출력시키세요.

case.2 숫자 범위의 문자가 발생시엔 숫자문자가 [문자(ascii 코드값)]입니다.
case.3 숫자와 영 대문자 사이에는 특문이 있는데, 그 경우경우, 숫자와 영대문자 사이의
특문[문자(ascii 코드값)] 입니다.
case.4 영대문자가 발생된 경우 영대문자 [문자(ascii 코드값)] 입니다.
case.5 영문자들(대,소문자) 사이에는 역시 특수문자가 있습니다. 이경우엔
영문들 사이의 [문자(ascii 코드값)] 입니다.
case.6 영소문자 인경우엔, 영소문자 [문자(ascii 코드값)] 입니다.
case.7 영소문자 이후에는 다시 특문이 존재 하는데, 이경우엔
영소문자 이후의 [문자(ascii 코드값)] 입니다.

이렇게 나오도록 만드세요.

참고로. ascii 코드 테이블을 보시면서 구분을 하세요.